import { OnboardingWizard } from '@/components/onboarding/OnboardingWizard';

export const metadata = { title: 'Crea tu plan — FORGE' };

export default function OnboardingPage() {
  return <OnboardingWizard />;
}
